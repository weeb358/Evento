# Values for a hosted Supabase project (Project Settings -> API).
# Used by `supabase link` and any local scripts — never commit the real .env.
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=
SUPABASE_ACCESS_TOKEN=
