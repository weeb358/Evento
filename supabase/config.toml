project_id = "events-platform-pk"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000"]
jwt_expiry = 3600
enable_signup = true

[auth.sms]
enable_signup = true
enable_confirmations = true
# Configure a real SMS provider (e.g. Twilio) before going to production —
# phone OTP is the only auth method in Phase 1. See docs/ARCHITECTURE.md for
# the OTP rate-limiting note.
[auth.sms.twilio]
enabled = false
account_sid = ""
auth_token = ""
message_service_sid = ""

[storage]
enabled = true
file_size_limit = "10MiB"

[realtime]
enabled = true
